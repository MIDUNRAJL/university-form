import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render( //to render react application...
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
